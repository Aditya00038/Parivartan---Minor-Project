// LightModeEnforcer has been disabled.
// The app uses dark: Tailwind classes and CSS variables defined in globals.css
// for proper theme support. This component is intentionally a no-op.
// It is kept to avoid import errors from any code that still references it.

export function LightModeEnforcer() {
  return null;
}
